1、在python中,字符串、列表、元组都属于序列,每个元素都有不用的序号,所以它们都是有序的

2、 + 运算符除了用于算数运算符外,还可以用于字符串、列表、元组的连接,但字符串不支持不同类型的对象之间相加或连接,
列表和元组支持

3、 * 运算符除了表示算术乘法,同样可用于字符串、列表、元组这几个序列类型与【整数】的乘法,表示序列元素的重复,
重新生成新的序列对象。但是字典和集合不支持与整数的相乘,因为其中的元素是不允许重复的

4、 切片操作,Python没有针对字符串的截取函数，只需要切片一个操作就可以完成，非常简单。
str[0:2]
list[0:2]
tuple[0:2]

只写[:]就可以原样复制一个str、list、tuple


5、相同的函数
1)、len()  len()表示大小,返回值为int
2)、max() min() 最大、最小,如果是对字符串操作,则是判断ASCII码的大小,ord函数可以查看ASCII的值
3)、
4)、